@using DiveDeepProject.Models

@model List<Category>

@section navbar {

}

@if (Model != null && Model.Count() > 0)
{
	<div class="row" style="gap: 15px;">

		@foreach (var category in Model)
		{
			<div class="card" style="width: 18rem;">
				<a class="btn btn-link" asp.asp-controller="categories" asp-action="categorylink" asp-route-id="@category.Id">
					<img src="~/lib/Public/LOGO.png" class="card-img-top" alt="category.Name">
					<div class="card-body">
						<h5 class="card-title">@category.Name</h5>
					</div>
				</a>
			</div>

		}
	</div>
}